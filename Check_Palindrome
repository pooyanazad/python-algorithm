# Buddy, this code checks if a string is a palindrome.
# A palindrome reads the same forward and backward.

def is_palindrome(s):
    return s == s[::-1]

# Example usage
print(is_palindrome("racecar"))  # Output: True
print(is_palindrome("python"))   # Output: False
