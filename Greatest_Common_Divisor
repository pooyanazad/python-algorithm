# Hi there, this code finds the GCD (Greatest Common Divisor)
# of two numbers using the Euclidean algorithm.

def gcd(a, b):
    while b != 0:
        a, b = b, a % b
    return a

# Example usage
print(gcd(54, 24))  # Output: 6
