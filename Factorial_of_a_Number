# Hi pal, this code computes the factorial of a given number n
# using a simple loop. Factorial of n = n * (n-1) * (n-2) * ... * 1

def factorial(n):
    if n < 0:
        return None  # Factorial is not defined for negative numbers
    result = 1
    for i in range(1, n+1):
        result *= i
    return result

# Example usage
print(factorial(5))  # Output: 120
